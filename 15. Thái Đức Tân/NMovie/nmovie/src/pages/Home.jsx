import React from "react";

import HeroSlide from "../components/hero-slide/HeroSlide";

const Home = (props) => {
  return (
    <div>
      <HeroSlide />
    </div>
  );
};

Home.propTypes = {};

export default Home;
